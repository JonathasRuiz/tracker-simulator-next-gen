apiVersion: v1
kind: Service
metadata:
  annotations:
    version: 1.34.0
  labels:
    app: trackerclient-go
  name: trackerclient-go
spec:
  ports:
    - name: http
      port: {{ .Values.trackerClientGo.port }}
      targetPort: {{ .Values.trackerClientGo.port }}
      protocol: TCP
  selector:
    app: trackerclient-go